using System;
using System.Collections.Generic;
using System.Linq;
using System.Text;
using Sudoku.data.Boards;

namespace Sudoku.controler
{
    public class SetUpController
    {
        // builder with inport strategy as options
        public SetUpController(string options) //Strategy
        {
            //switch with factory on create
            switch (options)
            {
                default:
                    return;
            }
        }
    }
}